describe("combo - flush", () => {
  test.todo("should return none if not enough cards")
  test.todo("should return none if not enough cards match the suit")
  test.todo("return a flush 5 cards have same suit")
  test.todo("return a flush of highest rank if more than 5 cards have same suit")
  test.todo("return flush of higher rank if cards can form 2 possible suit sets")
})