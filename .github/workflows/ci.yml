name: CI
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: install yarn
        run: npm install -g yarn
      - name: install dependencies
        run: yarn install
      - name: run test
        run: |
          yarn build
          yarn test
